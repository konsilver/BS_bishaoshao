server:
  port: 8082

spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.162.119.187:3306/bishaoshao?zeroDateTimeBehavior=convertToNull&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&autoReconnect=true&allowMultiQueries=true
    username: bishaoshao
    password: 123456
  data:
    redis:
      host: localhost
      port: 6379
      database: 1
  mail:
    host: smtp.qq.com 
    port: 587              # 587 是常用的 TLS 加密端口
    username: "2139752165@qq.com"  # 发件人邮箱地址
    password: "wjiehszvcrapeiga"  # 如果启用了两步验证，使用应用专用密码
    properties:
      mail:
        smtp:
          auth: true          # 启用身份验证
          starttls:
            enable: true      # 启用 STARTTLS 加密
      # 邮件发送者
    from: "2139752165@qq.com"  # 发件人邮箱

openai:
  api:
    key:

# jwt 相关设置
jwt:
    header: Authorization
    # 令牌前缀
    token-start-with: Bearer
    # token 有效时间
    token-validity-in-seconds: 7200000
    token-sign-key: "user"
    # 在线用户的 token
    online-key: "online-token:"
    # token 续期检查时间范围（毫秒），
    detect-time: 1800000
    # 续期时间范围，默认1小时，单位毫秒
    renew-time: 3600000

user-cache:
    validity-time-in-seconds: 7200000
    user-cache-key: "user-cache:"


# 验证码配置
captcha:
    # 验证码类型 circle-圆圈干扰验证码|gif-Gif验证码|line-干扰线验证码|shear-扭曲干扰验证码
    type: circle
    # 验证码宽度
    width: 120
    # 验证码高度
    height: 40
    # 验证码干扰元素个数
    interfere-count: 2
    # 文本透明度(0.0-1.0)
    text-alpha: 0.8
    # 验证码字符配置
    code:
      # 验证码字符类型 math-算术|random-随机字符
      type: math
      # 验证码字符长度，type=算术时，表示运算位数(1:个位数运算 2:十位数运算)；type=随机字符时，表示字符个数
      length: 1
    # 验证码字体
    font:
      # 字体名称 Dialog|DialogInput|Monospaced|Serif|SansSerif
      name: SansSerif
      # 字体样式 0-普通|1-粗体|2-斜体
      weight: 1
      # 字体大小
      size: 24
    # 验证码有效期(秒)
    expire-seconds: 60

